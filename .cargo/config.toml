# getrandom 0.3 needs both the `wasm_js` cargo feature (see Cargo.toml) AND this
# cfg flag to actually select the browser backend. Scoped to wasm so native
# builds are unaffected.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
