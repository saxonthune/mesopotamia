# Rust build artifacts
/target
# Standalone tool crates (e.g. tools/luminous-extractor) build here.
tools/*/target/
# The driftscape screensaver is its own standalone crate, off the game's build.
/driftscape/target

# Assembled static site (regenerated by build-web.sh)
/dist

# Per-user launch settings; usersettings.example.json is the committed template.
/usersettings.json

# Vendored upstream docs/source caches — raw input for condensed skills, not committed.
# The condensed skills live in .claude/skills/ and ARE committed.
.temp/


# Added by cargo
#
# already existing elements were commented out

#/target
